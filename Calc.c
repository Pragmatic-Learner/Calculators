#include <stdio.h>
char expression[49];
int main()
{
    printf("Expression MAXIMUM LENGTH: 49 CHARACTERS. !!BODMAS APPLIE!!.\n");
    printf("Operators :> ( || ) || ^ || / || * || + || -\n");
    printf("Enter : ");scanf("%s", expression);
    
    return 0;
}